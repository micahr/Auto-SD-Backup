# Core dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
paho-mqtt==1.6.1
httpx==0.26.0
click==8.1.7
pyyaml==6.0.1
jinja2==3.1.3
aiosqlite==0.19.0
python-multipart==0.0.6
smbprotocol==1.12.0
xxhash==3.4.1
psutil==5.9.8

# Platform-specific dependencies
# Linux only (for SD card detection and GPIO)
pyudev==0.24.1; sys_platform == 'linux'
RPi.GPIO==0.7.1; sys_platform == 'linux' and platform_machine == 'aarch64' or platform_machine == 'armv7l'

# macOS/development (for directory monitoring)
watchdog==3.0.0; sys_platform == 'darwin'