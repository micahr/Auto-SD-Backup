# Core dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
paho-mqtt==1.6.1
httpx==0.26.0
click==8.1.7
pyyaml==6.0.1
jinja2==3.1.3
aiosqlite==0.19.0
python-multipart==0.0.6
smbprotocol==1.12.0

# Platform-specific dependencies
# Linux only (for SD card detection)
pyudev==0.24.1; sys_platform == 'linux'

# macOS/development (for directory monitoring)
watchdog==3.0.0; sys_platform == 'darwin'

# Optional: install watchdog on all platforms for dev mode
# pip install watchdog
